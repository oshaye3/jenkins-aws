pipeline {

  /*
   * Run everything on an existing agent configured with a label 'jk-slave-agents'.
   */
  agent any

  stages {
    stage('Building the Report for AWS Services') {
      steps {
        dir("./lesson4") {
       sh '/jenkins-aws.sh'
       
        }
      }
    }
  }
  
  }
